# Session Report — Machine Cleanup & Capability Foundation

**Date:** 2026-07-14
**Duration:** ~5 hours
**Outcome:** Machine state reconciled with dotfiles, docs restructured, Bridge preserved, implementation frontier defined.

---

## Decisions Made

1. **Dotfiles is the god of truth.** `configuration.nix` + `home.nix` + `models.env` are canonical. GLOBAL.md, MACHINE.md, AGENTS.md document WHY and point at canonical sources. They never duplicate WHAT — that always drifts.

2. **`cu` → `cua`.** Cursor Agent launcher renamed. `/usr/bin/cu` (BSD call-up) restored. All docs, Bridge harness maps, and home.nix updated.

3. **Two-tier CLAUDE.md architecture.** `~/CLAUDE.md` → `GLOBAL.md` (machine constitution — hardware, gates, project map). `~/.claude/CLAUDE.md` → `AGENTS.md` (agent behavior — tool hierarchy, launchers, conventions).

4. **Global vs per-project packaging.** Brew = system runtimes + native CLIs. npm global = JS-based agent CLIs (launcher deps). uv tools = Python tools (daemon deps). Per-project = dev deps (eslint, prettier, pyright).

5. **Documentation principle.** Every fact lives in exactly one canonical file. Docs point at that file. When state changes, the canonical source changes and docs stay correct automatically.

---

## What Was Done

### Dotfiles structure
- Moved `captain/GLOBAL.md` → root `GLOBAL.md`
- Moved `captain/agent-rules/` → root `agent-rules/`
- Created `~/CLAUDE.md` → `GLOBAL.md` (was missing, referenced everywhere)
- Renamed `~/bin/cu` → `~/bin/cua` (fixes BSD collision)
- `captain/` now contains only launcher wrappers

### Removed (27 items)
- **Tools:** quota-fetch, mm-quota, secret-cache, no-mistakes (skill + binary + daemon + plist)
- **Voice:** voice-paste (plist + 3 binaries), voice toggle
- **Stale:** jsonl-watch, mlx-cleanup.py, tldr-code (55MB), tldr-daemon (42MB), scrapling
- **Orphans:** 5 .backup skill dirs, 8 ~/bin/.backup files, 3 orphan symlinks
- **Dead:** op wrapper, jw-cred-canary (script + plist), ~/projects/dotfiles (fake file)
- **Research:** chatgpt-bridge, gemini-bridge, perplexity-bridge (research-bridge not on disk)

### Home.nix: declared what was undeclared
- npm globals: 4 → 12 (+8 essential agent CLIs)
- uv tools: 4 → 6 (+cocoindex, +z3-solver)
- Symlinks: cua wrapper, ~/CLAUDE.md, agent-rules path, ghostty config

### Skills: 30 → 26 (all nix-managed)
Removed: cognee-quickstart, improve-codebase-architecture, setup-matt-pocock-skills, no-mistakes

### Docs restructured
- **GLOBAL.md:** moved to root, project map corrected (removed firstmate, added portfolio/quota-core), Local AI Stack points at models.env, Services section documents architecture not inventory
- **MACHINE.md:** 40 brews/9 casks, 12 npm globals, 26 skills, all stale claims fixed, gaps marked FIXED
- **TOOL_REGISTRY.md:** rewritten to match reality — pioneer REMOVED, cu→cua, stale launchers removed
- **AGENTS.md:** cu→cua, secret-cache→Bridge Keychain, ghost entries removed, bridge scripts removed
- **KNOWN_ISSUES.md:** fastedit decommissioned, bun removed, cu→cua
- **Bridge docs:** 7 quota-core references → "native Go provider framework"

### Bridge: provider framework preserved
- Dirty working tree committed (23 files, 2505+ lines)
- 13 provider fetchers: 6 committed, 6 new, 1 dormant (Pioneer — fails open)
- Pioneer wired in all 5 locations (HarnessToProvider, defaultProviders, secrets, CLI, tests)
- cua→cursor mapping added to HarnessToProvider + quota_test
- TLA+ model (85M+ states) + Lean 4 proofs (3 theorems) + gopter (8500 checks)
- 28 unit tests all pass, project builds

### Infrastructure fixes
- **Cognee:** crash loop since Jul 2 — fixed (Auth0 cloud auth → uvicorn local server + disable access control)
- **Lean:** 4.32.0 + Lake 5.0.0 installed via elan
- **Ghostty:** config symlinked into dotfiles + home.nix entry
- **Deny lists:** deployed to all three Claude config dirs
- **3 non-executable files:** com, ctx7, jw-restart → +x

### Verification (3 agents spawned)
- **Symlink audit:** 36 all resolve, 0 broken, 0 references to removed tools
- **Project audit:** 9 active repos on disk, all accounted in GLOBAL.md
- **Tool test:** 18/21 tools PASS (3 cold/limitation, not broken)

---

## Implementation Plan (from MACHINE_CAPABILITY_CONTRACT)

### Prerequisites (complete)
- [x] Portfolio machine inventory (#34)
- [x] Bridge provider preservation review (#88)

### First implementation slice (not started)

1. Generate Dotfiles Machine Capability Manifest
2. Create `cua` launcher — **DONE**
3. Preserve `/usr/bin/cu` and `/usr/bin/quota` — **cu DONE**
4. Retire duplicate collector names and stale documentation — **DONE**
5. Give Bridge one content-bound capability identity
6. Admit one declared capability
7. Deny one unmanaged capability
8. Spawn one governed worker
9. Capture full tool and effect traces
10. Record provider entitlement and actual usage receipts
11. Enforce documentation impact
12. Propagate the accepted projection to Portfolio
13. Reconcile direct or missing changes

### After the first pilot (not started)
- Proof-carrying CocoIndex pipeline
- Automatic generated documentation
- Verification Matrix historical experiment
- Tart provider experiment
- Orbit approvals and exception presentation
- Consumption forecasting (needs sufficient real receipts)
- Shadow comparison of routing and representation strategies

### Bridge code quality (not started)
- Fix `bridge quota` `os.Setenv` (Category B security concern)
- Refactor `Usage` struct (Entitlement vs Receipt vs Forecast vs Budget)
- DaytonaFetcher: remove or replace with config-backed stub
- Remove `_ = fmt.Sprintf` anti-pattern (7 files)
- Remove obsolete `"openai"` provider folder from secrets

### Loose ends (not started)
- `tldr warm ~/projects/bridge` (one-time cache build)
- `cd ~/projects/bridge/verification/lean && lake build` (verify Lean proofs compile)
- Archive or trash `~/projects/quota-core` (dead code)
- 5 pip→uv venvs from MACHINE.md migration plan
- Clone jw-core from GitHub (binaries survive without source)
- Squash 17 dotfiles auto-save commits

---

## Documentation Map

| File | Purpose | Audience |
|---|---|---|
| `configuration.nix` | Canonical: system-level config (LaunchAgents, Brew, nix-darwin) | Nix |
| `home.nix` | Canonical: user-level config (packages, symlinks, npm/uv tools) | Nix |
| `models.env` | Canonical: AI model paths and config | LaunchAgents + tools |
| `GLOBAL.md` | Machine constitution — hardware, gates, project map, architecture | Agents (via ~/CLAUDE.md) + human |
| `home/AGENTS.md` | Agent behavior — tool hierarchy, launchers, conventions | Agents (via ~/.claude/CLAUDE.md) |
| `MACHINE.md` | Audited manifest — what's installed, declared, and reproducible | Human |
| `TOOL_REGISTRY.md` | Tool status catalog — ACTIVE/REMOVED/UNVERIFIED | Human |
| `KNOWN_ISSUES.md` | BSD quirks, blocked tools, shell bypasses, workarounds | Agents + human |
