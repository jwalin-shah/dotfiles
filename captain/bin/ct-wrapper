#!/usr/bin/env python3
import os
import sys

os.execvp("secret-cache", [
    "secret-cache", "exec", "--",
    os.path.expanduser("~/bin/claude-launch"), "run", "token", "--dangerously-skip-permissions"
] + sys.argv[1:])
