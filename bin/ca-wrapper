#!/usr/bin/env bash
set -euo pipefail

# Permission bypass belongs to an explicitly admitted headless caller.  Do
# not force it here: direct `ca` and Bridge interactive tickets must retain
# Claude Code's native permission boundary.
CLAUDE_CONFIG_DIR="$HOME/.claude-a" \
ANTHROPIC_API_KEY="" \
ANTHROPIC_BASE_URL="" \
exec /opt/homebrew/bin/claude "$@"
